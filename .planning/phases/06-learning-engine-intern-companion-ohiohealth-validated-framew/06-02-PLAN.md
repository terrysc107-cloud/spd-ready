---
phase: 06-learning-engine-intern-companion-ohiohealth-validated-framew
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - spd-ready/src/lib/local-db/hld-questions.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The HLD domain has at least 10 new questions covering chemical sterilants, MEC monitoring, contact time, and rinse protocols"
    - "Each new HLD question is tagged with learning_domain='high_level_disinfection' and a concept_id from the HLD concept set"
  artifacts:
    - path: "spd-ready/src/lib/local-db/hld-questions.ts"
      provides: "10 High-Level Disinfection questions following TrackQuestion shape"
      contains: "export const HLD_QUESTIONS"
      min_lines: 200
  key_links:
    - from: "spd-ready/src/lib/local-db/hld-questions.ts"
      to: "spd-ready/src/lib/local-db/track-questions.ts"
      via: "import type TrackQuestion"
      pattern: "from './track-questions'"
---

<objective>
Author 10 new High-Level Disinfection (HLD) questions in a separate file to fill the case-study headline domain (D-05). HLD is the domain where OhioHealth saw the largest confidence gain (31% → 80%). Questions cover chemical sterilants (ortho-phthalaldehyde, glutaraldehyde, peracetic acid), MEC monitoring, contact time, rinse protocols, and scope reprocessing — paraphrased from AAMI ST91 and AORN guidelines.

Purpose: surface a meaningful HLD module in `/student/study` and give the mastery engine real signal in this domain.
Output: One new file `hld-questions.ts` exporting `HLD_QUESTIONS` as `TrackQuestion[]`. Authoring a separate file (rather than appending to `track-questions.ts`) keeps this plan parallel-safe with 06-01 — there is zero overlap on existing files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@spd-ready/AGENTS.md
@.planning/phases/06-learning-engine-intern-companion-ohiohealth-validated-framew/06-CONTEXT.md
@spd-ready/src/lib/local-db/track-questions.ts

<interfaces>
TrackQuestion shape (existing, src/lib/local-db/track-questions.ts):
```typescript
export type TrackQuestion = {
  id: string
  domain: TrackDomain  // legacy enum — set to closest match for compatibility
  difficulty: 'foundational' | 'intermediate' | 'advanced'
  question: string
  options: { A: string; B: string; C: string; D: string }
  correct: 'A' | 'B' | 'C' | 'D'
  partial_credit: 'A' | 'B' | 'C' | 'D' | null
  explanation: string
  image?: string
  judgment_type?: string
  real_world_standard?: string
  // Phase 6 additions (created in 06-01):
  legacy_domain?: TrackDomain
  learning_domain?: LearningDomain
  concept_id?: ConceptId
  error_categories?: ErrorCategory[]
}
```

HLD concept_ids from CONCEPT_CATALOG (created in 06-01):
- `concept-hld-chemical-sterilants`
- `concept-hld-mec-monitoring`
- `concept-hld-contact-time`
- `concept-hld-rinse-protocol`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 10 HLD questions</name>
  <read_first>
    - spd-ready/src/lib/local-db/track-questions.ts (TrackQuestion shape, existing question style — read first 200 lines for tone/length)
    - .planning/phases/06-learning-engine-intern-companion-ohiohealth-validated-framew/06-CONTEXT.md (D-05, specifics block at bottom)
  </read_first>
  <files>spd-ready/src/lib/local-db/hld-questions.ts (new)</files>
  <action>
Create file `spd-ready/src/lib/local-db/hld-questions.ts` exporting exactly 10 HLD questions. The file MUST start with this header:

```typescript
import type { TrackQuestion } from './track-questions'

/**
 * High-Level Disinfection (HLD) questions — Phase 6 content gap-fill.
 * Per D-05: ~10 new questions to support the OhioHealth case study's headline domain.
 * Sources paraphrased from AAMI ST91 and AORN scope reprocessing guidelines.
 *
 * Each question sets:
 *   - domain: 'STERILIZATION' (closest legacy enum, retained for compatibility)
 *   - learning_domain: 'high_level_disinfection' (Phase 6 framework)
 *   - concept_id: one of the four HLD concepts
 *   - error_categories: ['debris_found'] (HLD failures present as bioburden risk per DOMAIN_ERROR_MAP)
 */
export const HLD_QUESTIONS: TrackQuestion[] = [
  // ... 10 entries ...
]
```

Distribute the 10 questions across the four HLD concepts as follows:

- 3 questions on `concept-hld-chemical-sterilants` (ortho-phthalaldehyde / OPA, glutaraldehyde, peracetic acid — properties, indications, hazards)
- 3 questions on `concept-hld-mec-monitoring` (Minimum Effective Concentration, test strip use, when to discard solution)
- 2 questions on `concept-hld-contact-time` (per-product contact time at given temperature, consequences of under-immersion)
- 2 questions on `concept-hld-rinse-protocol` (post-HLD rinse with sterile or filtered water, drying, scope storage cabinet)

Question authoring constraints (apply to every entry):

- `id` format: `tq-hld-001` through `tq-hld-010` (zero-padded)
- `domain`: `'STERILIZATION'` (closest legacy enum — keeps the existing study UI working pre-remap)
- `legacy_domain`: omit (these are net-new, not remapped)
- `learning_domain`: `'high_level_disinfection'`
- `concept_id`: one of the four HLD concept IDs above
- `error_categories`: `['debris_found']`
- `difficulty`: at least 3 marked `'foundational'`, at least 4 marked `'intermediate'`, at least 2 marked `'advanced'`
- `question`: 1–3 sentences, scenario-flavored where natural (e.g., "A flexible endoscope completes manual cleaning. The HLD bath is OPA at 25°C. What is the minimum required immersion time?")
- `options`: exactly 4 (A-D), each plausible
- `correct`: single letter
- `partial_credit`: a near-correct distractor letter or `null`
- `explanation`: 1–3 sentences citing the relevant standard (e.g., "Per AAMI ST91, OPA requires 12 minutes at 20–25°C. Shorter times risk residual bioburden and downstream patient infection.")
- `real_world_standard`: a short citation tag like `'AAMI ST91'` or `'AORN: Flexible Endoscopes'`
- Do NOT copy text verbatim from any source — paraphrase. Do NOT include images.

Example template (the executor must produce 10 like this):

```typescript
{
  id: 'tq-hld-001',
  domain: 'STERILIZATION',
  learning_domain: 'high_level_disinfection',
  concept_id: 'concept-hld-chemical-sterilants',
  error_categories: ['debris_found'],
  difficulty: 'foundational',
  question: 'Which chemical is most commonly used for high-level disinfection of flexible endoscopes in modern SPDs?',
  options: {
    A: 'Hydrogen peroxide 3%',
    B: 'Ortho-phthalaldehyde (OPA)',
    C: 'Isopropyl alcohol 70%',
    D: 'Sodium hypochlorite',
  },
  correct: 'B',
  partial_credit: null,
  explanation: 'OPA is the predominant high-level disinfectant for flexible endoscopes due to its rapid action at room temperature and broad microbicidal spectrum. Per AAMI ST91, alcohol and bleach are not used for HLD of patient-contact instruments.',
  real_world_standard: 'AAMI ST91',
},
```
  </action>
  <verify>
    <automated>cd spd-ready && npx tsc --noEmit src/lib/local-db/hld-questions.ts && node -e "const {HLD_QUESTIONS} = require('./src/lib/local-db/hld-questions.ts'); console.log(HLD_QUESTIONS.length)" 2>&1 | grep -E '^(10|error)'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `spd-ready/src/lib/local-db/hld-questions.ts`
    - Export `HLD_QUESTIONS` is an array with exactly 10 entries (grep `id: 'tq-hld-` count = 10)
    - All entries have `learning_domain: 'high_level_disinfection'` (grep count = 10)
    - All entries have `error_categories: ['debris_found']` (grep count = 10)
    - All four HLD concept_ids appear: `concept-hld-chemical-sterilants` (3 occurrences), `concept-hld-mec-monitoring` (3), `concept-hld-contact-time` (2), `concept-hld-rinse-protocol` (2)
    - Difficulty distribution: at least 3 `foundational`, at least 4 `intermediate`, at least 2 `advanced`
    - Each entry has a `real_world_standard` field set to an AAMI or AORN citation string
    - `npx tsc --noEmit` passes
    - File length >= 200 lines (10 entries × ~20 lines each)
  </acceptance_criteria>
  <done>10 HLD questions are authored, type-check, and follow the citation discipline.</done>
</task>

</tasks>

<verification>
- File compiles
- Question count is exactly 10
- Concept distribution matches plan (3/3/2/2)
- Difficulty mix satisfies minimums
</verification>

<success_criteria>
- HLD_QUESTIONS can be imported and merged with TRACK_QUESTIONS by downstream UI
- Content reads as authentic SPD knowledge, not generic
</success_criteria>

<output>
After completion, create `.planning/phases/06-learning-engine-intern-companion-ohiohealth-validated-framew/06-02-SUMMARY.md`
</output>
