/**
 * Project-wide Lucide icon barrel.
 *
 * Always import icons from here — never from 'lucide-react' directly.
 * This gives us one place to swap icons site-wide and keeps the tree-shake
 * surface predictable.
 *
 * Naming follows the semantic role on screen, not the glyph shape, so
 * swapping the underlying icon never breaks call sites.
 */
export {
  // ── Navigation ────────────────────────────────────────────────────
  ArrowLeft,
  ArrowRight,
  ArrowUpRight,
  ChevronDown,
  ChevronLeft,
  ChevronRight,
  ChevronUp,
  Menu as MenuIcon,
  X as CloseIcon,
  ExternalLink,

  // ── Status ────────────────────────────────────────────────────────
  CheckCircle2 as CheckCircle,
  XCircle,
  AlertCircle,
  AlertTriangle,
  Info,
  Loader2 as Spinner,

  // ── Product domains ───────────────────────────────────────────────
  Hospital,
  GraduationCap,
  BookOpen,
  ClipboardList,
  Clipboard,
  Target,
  Gauge,
  BarChart3,
  TrendingUp,
  TrendingDown,
  Brain,
  Trophy,
  Flame,
  Zap,
  Award,
  ShieldCheck,
  Stethoscope,

  // ── User / identity ───────────────────────────────────────────────
  User,
  UserPlus,
  Users,
  LogOut,
  LogIn,
  Settings,

  // ── Actions ───────────────────────────────────────────────────────
  Pencil,
  Plus,
  Minus,
  Search,
  Filter,
  RefreshCcw,
  Workflow,
  FolderOpen,
  FileText,
  Send,
  Eye,
  EyeOff,
  Bookmark,
  MoreHorizontal,
  MoreVertical,

  // ── Meta / time ───────────────────────────────────────────────────
  Calendar,
  Clock,
  MapPin,
  Sun,
  Moon,
  Sparkles,
} from 'lucide-react'
